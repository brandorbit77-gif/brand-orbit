<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>BRAND ORBIT - Creative Growth Studio</title>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=DM+Mono&family=Manrope:wght@400;500;600;700;800&display=swap" rel="stylesheet">

  <style>
    :root {
      --black: #080808;
      --card: #101010;
      --line: #282828;
      --cream: #f7f5ef;
      --muted: #a5a5a5;
      --lime: #d7ff46;
      --violet: #8f7bff;
    }

    * { box-sizing: border-box; }
    html { scroll-behavior: smooth; }

    body {
      margin: 0;
      background: var(--black);
      color: var(--cream);
      font-family: Manrope, Arial, sans-serif;
    }

    a {
      color: inherit;
      text-decoration: none;
    }

    .container {
      width: min(1160px, calc(100% - 40px));
      margin: auto;
    }

    .mono, .kicker, .number {
      font: 11px "DM Mono", monospace;
      letter-spacing: .12em;
      text-transform: uppercase;
    }

    .nav {
      height: 82px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      border-bottom: 1px solid var(--line);
    }

    .brand {
      font-weight: 800;
      letter-spacing: -1px;
      font-size: 19px;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .mark {
      width: 17px;
      height: 17px;
      border-radius: 50%;
      border: 4px solid var(--lime);
      box-shadow: 0 0 0 3px #d7ff4630;
    }

    .nav-links {
      display: flex;
      gap: 28px;
      font-size: 13px;
      color: #c8c8c8;
      font-weight: 700;
    }

    .nav-cta {
      border: 1px solid #5b5b5b;
      padding: 11px 16px;
      font-size: 12px;
      border-radius: 999px;
      font-weight: 800;
      transition: .2s;
    }

    .nav-cta:hover {
      background: var(--lime);
      border-color: var(--lime);
      color: #090909;
    }

    .hero {
      min-height: 695px;
      position: relative;
      overflow: hidden;
      border-bottom: 1px solid var(--line);
      display: flex;
      align-items: center;
    }

    .hero:before {
      content: "";
      position: absolute;
      width: 680px;
      height: 680px;
      border-radius: 50%;
      right: -175px;
      top: -210px;
      background: radial-gradient(circle, #8f7bff35, transparent 65%);
    }

    .hero:after {
      content: "";
      position: absolute;
      width: 600px;
      height: 400px;
      left: 15%;
      bottom: -330px;
      border-radius: 50%;
      background: radial-gradient(circle, #d7ff4630, transparent 65%);
    }

    .hero-grid {
      position: relative;
      z-index: 1;
      display: grid;
      grid-template-columns: 1.15fr .85fr;
      gap: 50px;
      align-items: center;
      padding: 80px 0;
    }

    .eyebrow {
      font: 11px "DM Mono", monospace;
      color: var(--lime);
      letter-spacing: .13em;
      text-transform: uppercase;
      display: flex;
      align-items: center;
      gap: 9px;
    }

    .eyebrow:before {
      content: "";
      height: 7px;
      width: 7px;
      background: var(--lime);
      border-radius: 50%;
      box-shadow: 0 0 13px var(--lime);
    }

    h1 {
      font-size: clamp(53px, 7.2vw, 96px);
      line-height: .93;
      letter-spacing: -.075em;
      margin: 22px 0 25px;
      font-weight: 800;
    }

    h1 .outline {
      color: transparent;
      -webkit-text-stroke: 1.5px var(--cream);
    }

    .hero-copy {
      line-height: 1.7;
      color: #b7b7b7;
      font-size: 17px;
      max-width: 510px;
      margin: 0 0 32px;
    }

    .buttons {
      display: flex;
      gap: 12px;
      flex-wrap: wrap;
    }

    .button {
      padding: 15px 20px;
      border-radius: 4px;
      font-size: 13px;
      font-weight: 800;
      display: inline-flex;
      gap: 8px;
      align-items: center;
      transition: .2s;
    }

    .primary {
      background: var(--lime);
      color: #101010;
    }

    .primary:hover {
      transform: translateY(-3px);
      box-shadow: 0 9px 25px #d7ff4645;
    }

    .secondary {
      border: 1px solid #454545;
      color: #eee;
    }

    .hero-art {
      height: 410px;
      position: relative;
    }

    .orb {
      position: absolute;
      border: 1px solid #f6f3ed70;
      border-radius: 50%;
      inset: 18px;
    }

    .orb:before,
    .orb:after {
      content: "";
      position: absolute;
      border-radius: 50%;
      border: 1px solid #d7ff4669;
    }

    .orb:before { inset: 48px; }

    .orb:after {
      inset: 101px;
      border-color: #8f7bff8a;
    }

    .core {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 96px;
      height: 96px;
      border-radius: 50%;
      background: var(--lime);
      box-shadow: 0 0 55px #d7ff4694;
    }

    .sat {
      position: absolute;
      width: 16px;
      height: 16px;
      border-radius: 50%;
      background: var(--cream);
      box-shadow: 0 0 25px #fff;
    }

    .one {
      top: 47px;
      right: 69px;
    }

    .two {
      bottom: 43px;
      left: 83px;
      background: var(--violet);
      box-shadow: 0 0 25px var(--violet);
    }

    .trusted {
      position: absolute;
      bottom: 22px;
      left: 0;
      background: #111111d6;
      backdrop-filter: blur(8px);
      border: 1px solid #303030;
      padding: 16px 18px;
      display: flex;
      gap: 14px;
      align-items: center;
    }

    .faces {
      display: flex;
    }

    .face {
      width: 27px;
      height: 27px;
      border: 2px solid #111;
      border-radius: 50%;
      margin-left: -7px;
      background: linear-gradient(145deg, #9c6237, #f3cf9d);
    }

    .face:first-child {
      margin: 0;
      background: linear-gradient(145deg, #5d4a38, #d3b49b);
    }

    .face:last-child {
      background: var(--violet);
    }

    .trusted b {
      display: block;
      font-size: 12px;
    }

    .trusted small {
      display: block;
      color: #999;
      font-size: 10px;
      margin-top: 2px;
    }

    .ticker {
      overflow: hidden;
      border-bottom: 1px solid var(--line);
      white-space: nowrap;
      padding: 17px 0;
      color: #b9b9b9;
      font: 11px "DM Mono", monospace;
      text-transform: uppercase;
      letter-spacing: .14em;
    }

    .ticker span { padding: 0 22px; }

    .ticker i {
      font-style: normal;
      color: var(--lime);
      font-size: 15px;
    }

    section { padding: 112px 0; }

    .section-top {
      display: flex;
      justify-content: space-between;
      gap: 30px;
      align-items: end;
      margin-bottom: 48px;
    }

    .kicker { color: var(--lime); }

    .section-title {
      font-size: clamp(33px, 4.2vw, 56px);
      letter-spacing: -.06em;
      line-height: 1;
      margin: 13px 0 0;
    }

    .section-intro {
      color: var(--muted);
      font-size: 15px;
      line-height: 1.7;
      max-width: 355px;
      margin: 0;
    }

    .services {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      border-left: 1px solid var(--line);
      border-top: 1px solid var(--line);
    }

    .service {
      min-height: 275px;
      padding: 26px;
      border-right: 1px solid var(--line);
      border-bottom: 1px solid var(--line);
      transition: .25s;
      position: relative;
    }

    .service:hover { background: #141414; }

    .number { color: #777; }

    .service h3 {
      font-size: 21px;
      letter-spacing: -.04em;
      margin: 68px 0 11px;
    }

    .service p,
    .step p {
      font-size: 13px;
      line-height: 1.7;
      color: #aaa;
      margin: 0;
    }

    .arrow {
      position: absolute;
      right: 25px;
      bottom: 24px;
      color: var(--lime);
      font-size: 23px;
    }

    .work {
      background: #0d0d0d;
      border-top: 1px solid var(--line);
      border-bottom: 1px solid var(--line);
    }

    .work-grid {
      display: grid;
      grid-template-columns: 1.4fr 1fr;
      gap: 18px;
    }

    .project {
      background: #161616;
      min-height: 261px;
      overflow: hidden;
      position: relative;
      padding: 23px;
      display: flex;
      align-items: end;
    }

    .project.large {
      min-height: 540px;
      background: linear-gradient(130deg, #172b2e, #0b181a);
    }

    .project:nth-child(2) {
      background: linear-gradient(135deg, #382836, #110c10);
    }

    .project:nth-child(3) {
      background: linear-gradient(135deg, #2d290e, #121103);
    }

    .project:before {
      content: "";
      position: absolute;
      inset: 0;
      opacity: .65;
      transition: .35s;
    }

    .project.large:before {
      background:
        radial-gradient(circle at 67% 35%, #d7ff46 0 2%, transparent 2.5%),
        radial-gradient(circle at 68% 35%, transparent 0 20%, #d7ff4644 20.4% 20.8%, transparent 21.1% 34%, #d7ff4627 34.4% 34.7%, transparent 35%);
    }

    .project:nth-child(2):before {
      background:
        radial-gradient(circle at 65% 40%, #957bff 0 7%, transparent 7.4%),
        radial-gradient(circle at 65% 40%, transparent 0 23%, #957bff59 23.3% 23.7%, transparent 24%);
    }

    .project:nth-child(3):before {
      background:
        linear-gradient(45deg, transparent 40%, #d7ff4626 40.5% 42%, transparent 42.5%),
        linear-gradient(-45deg, transparent 43%, #d7ff4650 43.5% 45%, transparent 45.5%);
    }

    .project:hover:before { transform: scale(1.08); }

    .project-info {
      position: relative;
      z-index: 1;
    }

    .project-info small {
      font: 10px "DM Mono", monospace;
      color: #d7ff46;
      text-transform: uppercase;
      letter-spacing: .1em;
    }

    .project-info h3 {
      font-size: 24px;
      letter-spacing: -.05em;
      margin: 8px 0 0;
    }

    .project-side {
      display: grid;
      gap: 18px;
    }

    .process {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      border-top: 1px solid var(--line);
      border-left: 1px solid var(--line);
    }

    .step {
      padding: 25px;
      min-height: 190px;
      border-bottom: 1px solid var(--line);
      border-right: 1px solid var(--line);
    }

    .step span {
      display: block;
      color: var(--lime);
      font: 12px "DM Mono", monospace;
    }

    .step h3 {
      font-size: 17px;
      margin: 40px 0 8px;
    }

    .contact {
      padding: 118px 0;
      background: var(--lime);
      color: #0c0c0c;
      position: relative;
      overflow: hidden;
    }

    .contact:after {
      content: "*";
      position: absolute;
      right: 8%;
      bottom: -75px;
      font-size: 400px;
      line-height: 1;
      color: #0000000c;
    }

    .contact-inner {
      position: relative;
      z-index: 1;
      display: flex;
      justify-content: space-between;
      align-items: end;
      gap: 30px;
    }

    .contact h2 {
      font-size: clamp(46px, 6.4vw, 85px);
      letter-spacing: -.075em;
      line-height: .92;
      margin: 16px 0 0;
    }

    .contact p {
      max-width: 370px;
      font-size: 16px;
      line-height: 1.7;
      margin: 0 0 21px;
    }

    .contact .button {
      background: #0c0c0c;
      color: #fff;
      border-radius: 3px;
    }

    footer {
      padding: 29px 0;
      color: #898989;
      font-size: 11px;
    }

    footer .container {
      display: flex;
      justify-content: space-between;
      gap: 15px;
    }

    @media (max-width: 760px) {
      .container { width: min(100% - 28px, 1160px); }
      .nav { height: 67px; }
      .nav-links { display: none; }
      .nav-cta { font-size: 10px; padding: 9px 12px; }
      .hero { min-height: auto; }

      .hero-grid {
        grid-template-columns: 1fr;
        padding: 76px 0 43px;
        gap: 35px;
      }

      .hero-art {
        height: 290px;
        max-width: 360px;
        margin: auto;
        width: 100%;
      }

      h1 { font-size: 59px; }
      .hero-copy { font-size: 15px; }
      .trusted { bottom: 0; }

      .section-top {
        display: block;
        margin-bottom: 30px;
      }

      .section-intro { margin-top: 20px; }
      .services, .process { grid-template-columns: 1fr; }
      .service { min-height: 205px; }
      .service h3 { margin-top: 38px; }
      .work-grid { grid-template-columns: 1fr; }
      .project.large { min-height: 380px; }
      .work { padding: 70px 0; }
      .contact { padding: 80px 0; }
      .contact-inner { display: block; }
      .contact p { margin-top: 24px; }
      .contact h2 { font-size: 59px; }
      section { padding: 72px 0; }

      footer .container {
        flex-direction: column;
      }

      .orb:after { inset: 71px; }
      .orb:before { inset: 34px; }
    }
  </style>
</head>

<body>
  <header class="container nav">
    <a href="#" class="brand">
      <i class="mark"></i>
      BRAND ORBIT
    </a>

    <nav class="nav-links">
      <a href="#services">Services</a>
      <a href="#work">Work</a>
      <a href="#process">Process</a>
    </nav>

    <a class="nav-cta" href="https://wa.me/917337748121?text=Hi%20Brand%20Orbit%2C%20I%20want%20to%20grow%20my%20brand.">
      Let's talk &rarr;
    </a>
  </header>

  <main>
    <section class="hero">
      <div class="container hero-grid">
        <div>
          <div class="eyebrow">Creative growth studio</div>

          <h1>
            MAKE YOUR BRAND<br>
            <span class="outline">IMPOSSIBLE</span><br>
            TO IGNORE.
          </h1>

          <p class="hero-copy">
            We turn scrolling into stopping, watching into wanting, and attention into growth - with strategy, content and video that hit differently.
          </p>

          <div class="buttons">
            <a class="button primary" href="https://wa.me/917337748121?text=Hi%20Brand%20Orbit%2C%20I%20want%20to%20grow%20my%20brand.">
              Start a project &rarr;
            </a>

            <a class="button secondary" href="#work">
              See our work &darr;
            </a>
          </div>
        </div>

        <div class="hero-art">
          <div class="orb"></div>
          <div class="core"></div>
          <i class="sat one"></i>
          <i class="sat two"></i>

          <div class="trusted">
            <div class="faces">
              <i class="face"></i>
              <i class="face"></i>
              <i class="face"></i>
            </div>

            <div>
              <b>Built for brands with ambition</b>
              <small>Strategy &middot; Content &middot; Growth</small>
            </div>
          </div>
        </div>
      </div>
    </section>

    <div class="ticker">
      <span>Social media strategy</span><i>&#10022;</i>
      <span>Video editing</span><i>&#10022;</i>
      <span>Content creation</span><i>&#10022;</i>
      <span>Brand growth</span><i>&#10022;</i>
      <span>Social media strategy</span><i>&#10022;</i>
      <span>Video editing</span><i>&#10022;</i>
    </div>

    <section id="services">
      <div class="container">
        <div class="section-top">
          <div>
            <div class="kicker">What we do</div>
            <h2 class="section-title">Your brand, in<br>its best light.</h2>
          </div>

          <p class="section-intro">
            One focused creative partner for the content, systems and ideas that make your brand move forward.
          </p>
        </div>

        <div class="services">
          <article class="service">
            <span class="number">01 / Social</span>
            <h3>Social Media<br>Management</h3>
            <p>Clear strategy, consistent posting and a feed that actually feels like your brand.</p>
            <b class="arrow">&rarr;</b>
          </article>

          <article class="service">
            <span class="number">02 / Edit</span>
            <h3>Video Editing<br>&amp; Reels</h3>
            <p>Fast-paced, platform-ready videos designed to win attention in the first second.</p>
            <b class="arrow">&rarr;</b>
          </article>

          <article class="service">
            <span class="number">03 / Create</span>
            <h3>Content<br>Creation</h3>
            <p>Scroll-stopping posts, carousels and campaigns made for your audience.</p>
            <b class="arrow">&rarr;</b>
          </article>
        </div>
      </div>
    </section>

    <section class="work" id="work">
      <div class="container">
        <div class="section-top">
          <div>
            <div class="kicker">Selected work</div>
            <h2 class="section-title">Made to make<br>an impact.</h2>
          </div>

          <p class="section-intro">
            A glimpse of the bold direction we bring to every project.
          </p>
        </div>

        <div class="work-grid">
          <article class="project large">
            <div class="project-info">
              <small>Social media campaign</small>
              <h3>Future Forward</h3>
            </div>
          </article>

          <div class="project-side">
            <article class="project">
              <div class="project-info">
                <small>Video content</small>
                <h3>Made in motion</h3>
              </div>
            </article>

            <article class="project">
              <div class="project-info">
                <small>Brand identity</small>
                <h3>Built to stand out</h3>
              </div>
            </article>
          </div>
        </div>
      </div>
    </section>

    <section id="process">
      <div class="container">
        <div class="section-top">
          <div>
            <div class="kicker">The Orbit method</div>
            <h2 class="section-title">Simple process.<br>Serious results.</h2>
          </div>
        </div>

        <div class="process">
          <article class="step">
            <span>01</span>
            <h3>Discover</h3>
            <p>We understand your brand, audience and goals.</p>
          </article>

          <article class="step">
            <span>02</span>
            <h3>Build</h3>
            <p>We map the strategy and create the visual direction.</p>
          </article>

          <article class="step">
            <span>03</span>
            <h3>Create</h3>
            <p>We produce content that earns attention.</p>
          </article>

          <article class="step">
            <span>04</span>
            <h3>Grow</h3>
            <p>We learn, improve and keep your brand moving.</p>
          </article>
        </div>
      </div>
    </section>

    <section class="contact">
      <div class="container contact-inner">
        <div>
          <div class="kicker" style="color:#202020">Ready when you are</div>
          <h2>LET'S PUT YOUR<br>BRAND IN ORBIT.</h2>
        </div>

        <div>
          <p>
            Have a project in mind? Tell us where your brand wants to go. We'll help it get there.
          </p>

          <a class="button" href="https://wa.me/917337748121?text=Hi%20Brand%20Orbit%2C%20I%20want%20to%20grow%20my%20brand.">
            Chat on WhatsApp &rarr;
          </a>
        </div>
      </div>
    </section>
  </main>

  <footer>
    <div class="container">
      <span>&copy; 2026 BRAND ORBIT. ALL RIGHTS RESERVED.</span>
      <span class="mono">CREATE / CAPTURE / CONVERT</span>
    </div>
  </footer>
</body>
</html>